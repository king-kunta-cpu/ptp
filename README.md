# king-kunta-cpu/ptp

Cedar Loom — autonomous asset factory.

- `v2/` — **Cedar Loom v2.0 FACTORY (current)**: real Notion manufacturing, honest claims,
  9 transparent niche sub-brands, Whop + Lightning rails. Start at `v2/docs/SETUP-v2.md`.
- `cedar-loom/` — v1.7-era factory versions, guides, and the legacy GitHub Actions job
  (kept for reference; superseded by v2).
- client-* folders — v1.7 client landing pages.
- `.github/workflows/v2-factory.yml` — v2 manufacturing + weekend video jobs.

No secrets live in this repo: all keys come from the Config sheet / GitHub Actions secrets /
Cloudflare worker secrets.
